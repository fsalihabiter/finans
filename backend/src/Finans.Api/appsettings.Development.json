{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Database": {
    "_comment": "Yerel geliştirme kolaylığı: `dotnet run` açılışta şemayı migrate eder ve seed'ler (idempotent). Prod appsettings.json'da kapalı kalır.",
    "ApplyMigrationsOnStartup": true,
    "Seed": true
  },
  "Cors": {
    "_comment": "Vite dev sunucusu origin'leri (5173 başkasıysa 5174'e kayar; 5180 screenshot).",
    "AllowedOrigins": [
      "http://localhost:5173",
      "http://localhost:5174",
      "http://localhost:5180"
    ]
  }
}
