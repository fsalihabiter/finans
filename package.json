{
  "name": "finans-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Nirengi — portfolio tracking & investment education app. Monorepo: .NET 10 API + React web (primary) + shared @finans/shared package (mobile later).",
  "packageManager": "pnpm@11.5.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev:web": "pnpm --filter @finans/web dev",
    "lint": "pnpm -r lint"
  }
}
