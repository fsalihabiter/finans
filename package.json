{
  "name": "finans-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Finans / Portföy uygulaması — monorepo (web öncelikli, mobil sonra, paylaşılan @finans/shared)",
  "packageManager": "pnpm@11.5.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev:web": "pnpm --filter @finans/web dev",
    "lint": "pnpm -r lint"
  }
}
