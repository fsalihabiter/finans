# Grafana datasource provisioning (T2.8). Prometheus iç ağda 9090'da.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
