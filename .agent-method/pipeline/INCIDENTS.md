# INCIDENTS — problems discovered outside planned work
| ID | Date | Summary | Severity | Linked GD/D | Status |
|---|---|---|---|---|---|
| INC-003 | 2026-09-21 | **Nakit pozisyonunun `CurrentPrice`'ı NULL → liste satırında değer ve getiri "—".** Kök neden: API'den oluşturulan HER pozisyon `CurrentPrice = null` başlar; nakitte arayüz fiyat butonunu gizlediği için ("sabit ₺1") kullanıcı bunu hiç düzeltemez. Tohum 1 yazdığı için testler görmedi. ⚠ **İlk teşhis yanlıştı (düzeltildi):** "kâr ~34 bin eksik" DEĞİL — özet fiyatı olmayan pozisyonu **maliyetinden** değere katıyor, yani toplam ve kâr doğruydu (özet 759.165,52 = görünen satırlar 724.816,02 + nakit 34.349,50). Gerçek sorun **tutarsız görüntü**: toplam, görünen satırların toplamına eşit değildi. **Düzeltme (2026-09-21):** `AssetPricing.FixedUnitPriceFor` (nakit = 1) okuma yolunda (liste · özet · seri) + oluşturmada uygulanıyor; `CashFixedUnitPrice` migration'ı saklanan NULL'ları 1 yaptı. Canlıda: görünen satırlar toplamı = özet = 759.165,52. Regresyon testi API yolunu deniyor (tohum değil). | medium | GD-002 (keşif) | fixed — kabul bekliyor |
| INC-002 | 2026-09-20 | **INV-04 ile `review` durumu çelişiyor (çerçeve sorunu, ürün değil):** INV-04 "`active.gd_id` işaret ettiği GD'nin status'ü `in-progress` olmalı" diyor; oysa `_GD_TEMPLATE.md` `review` durumunu meşru bir aşama sayıyor ve `adm-close` GD'yi `review`'e taşıyabiliyor. GD-001 şu an `review` → hook/doctor uyarı veriyor. **Sessizce düzeltilmedi:** `gd_id`'yi boşaltmak aktif döngü bilgisini kaybettirirdi. Muhtemel çözüm: INV-04 ifadesi `in-progress | review` olacak şekilde genişletilsin (ADM çerçeve kararı, bu proje dışı). | low | GD-001 | open |
| INC-001 | 2026-09-20 | **4 entegrasyon testi ortam hatasıyla kırmızı:** `HealthEndpointTests` (1) + `ObservabilitySecurityTests` (3) → `InvalidOperationException: The server has not been started or no web application was configured` (`WebApplicationFactory`/`TestServer` ayağa kalkmıyor). **Kapsam dışı ve önceden var:** aynı 4 test HEAD'de (`30c0bc9`, temiz worktree) de kırmızı — GD-001 değişiklikleriyle ilgisiz. TASKLOG 2026-07-26 girdisinde de aynı hata "bilinen ortam hatası" olarak geçiyor. ⚠ Bu **test borcu değildir** (INV-08): kapsam eksik değil, koşum ortamı bozuk. Sağlık/gözlemlenebilirlik uçları bu testlerle korunuyordu → şu an korumasız. | high | — | open |
