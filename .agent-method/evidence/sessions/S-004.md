---
id: S-004
runtime: claude-code
started_at: null      # adm-open çalıştırılmadı → oturum açılışı kaydedilmedi (asgari kanıt notu)
closed_at: "2026-09-22T22:22:09Z"
start_sha: 3464759
end_sha: 200bc89
active_gd: null
---

# S-004 — Canlı demo: web Vercel, API + PostgreSQL Render (asgari kanıt notu)

> ⚠ Bu oturum `adm-open` ile açılmadı; `state.yaml → session` boştu. `adm-close`
> ön koşulu sağlanmadığı için bu dosya, kullanıcı onayıyla yazılmış **asgari kanıt
> notudur**. Başlangıç zamanı kaydedilmediği için boş bırakıldı. Başlangıç SHA'sı
> oturum başındaki temiz HEAD'dir (`3464759`).

## Hedef
Vercel'e deploy edilen web (`finans-web-seven.vercel.app`) "Backend'e ulaşılamadı"
hatası veriyordu. Amaç sitenin canlıda çalışması.

## Teşhis
- `web/src/lib/api.ts` `baseUrl: ""` ile istekleri aynı origin'e gönderir. Geliştirmede
  bunları Vite proxy'si, compose'da Caddy iletiyordu. Vercel'de ikisi de yok → `/api/health` 404.
- Vercel .NET çalıştıramaz. API'nin ayrı bir yerde barındırılması gerekiyordu.

## Yapılan iş (commit'ler `3464759..200bc89`)
- **`6a3a7ad`** — feat(deploy):
  - `render.yaml`: Render Blueprint (Docker API + ücretsiz PostgreSQL, Frankfurt).
    Açılışta migration + seed çalışır, `Auth__DevUserId` seed kullanıcısıdır.
    Sırlar `sync: false`, repoda yok.
  - `web/vercel.json`: `/api/*` isteklerini Render'a ileten rewrite + SPA fallback + asset cache başlığı.
  - `PostgresConnectionString.Normalize` (Infrastructure/Persistence): Render'ın
    `postgresql://` URI'sini Npgsql biçimine çevirir. `Program.cs` bunu kullanıyor.
  - `D-020-demo-hosting-vercel-render.md` kararı yazıldı.
  - `INCIDENTS.md` → INC-001'e kök neden adayı eklendi (aşağıda).
- **`200bc89`** — fix(deploy): `finans-api.onrender.com` başka bir servise ait çıktı.
  Render ad çakışmasında `finans-api-gctu` adını verdi; rewrite hedefi düzeltildi.

## Doğrulama
- **Birim:** `PostgresConnectionStringTests` 3/3 yeşil (URI → Npgsql, varsayılan port +
  sslmode, anahtar=değer olduğu gibi kalır).
- **Entegrasyon:** 184 yeşil, 4 kırmızı. Kırmızılar `HealthEndpointTests` (1) ve
  `ObservabilitySecurityTests` (3); **değişiklikten önce de kırmızıydılar** (stash ile
  doğrulandı) → INC-001, bu oturumun gerilemesi değil.
- **Canlı (2026-09-22):**
  - `finans-api-gctu.onrender.com/health` → `Healthy`
  - `finans-web-seven.vercel.app/api/health` → `{"status":"ok"}`
  - `/api/portfolio/summary` → 200
  - `/varliklar` doğrudan açılıyor → 200
  - Kullanıcı sitenin çalıştığını onayladı.

## Review
⚠ Backend kodu değişti (`Program.cs` + yeni `PostgresConnectionString`) ama
`adm-review` çalışmadı. Değişiklik küçük ve testli; review gerekip gerekmediğine insan karar verir.

## Açık konular
- **Güvenlik (D-020):** kimlik doğrulama yok (Faz 7). Render adresini bilen herkes tek
  kullanıcının verisini okuyup yazabilir. Canlıda yalnız seed/demo veri kullanılmalı.
  Render adresinde `/metrics` herkese açık.
- **Ücretsiz katman:** servis 15 dk boşta kalınca uyur. Ücretsiz Postgres 30 gün
  sonra silinir (Render e-postayla uyarır).
- **INC-001 (high, açık):** kök neden adayı — sade `WebApplicationFactory` Development
  ortamında açılışta migration yapıyor; yerel PostgreSQL kapalıyken host başlamıyor.
  Çözüm önerisi INCIDENTS'ta.

## Gözlendi, bu oturuma ait değil
Yok. Oturum başında çalışma ağacı temizdi.

## Sıradaki adım
**GD-003 — S0-L8 "Vade, hedef ve portföy"** (POOL'dan; kullanıcı seçimi 2026-09-22).
Bir sonraki oturum `adm-open` ile açılmalı.
